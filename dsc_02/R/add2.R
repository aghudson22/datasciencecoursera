add2 <- function(x, y) { # define function name, list formal arguments
    # x - numeric vector
    # y - numeric vector
    x + y # define function body - add two inputs
} # end function; last line calculated, or contents of return(), will be returned by function
